from setuptools import setup

setup(name='gaussian-binomial-distributions',
      version='1.0',
      description='Gaussian distributions',
      packages=['gaussian-binomial-distributions'],
      zip_safe=False)
